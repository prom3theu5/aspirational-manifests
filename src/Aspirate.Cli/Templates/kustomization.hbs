resources:
{{#each manifests}}
- {{this}}
{{/each}}

{{#if isService}}
configMapGenerator:
- name: {{name}}-env
  literals:
  {{#each env}}
    - {{@key}}={{this}}
  {{/each}}
    - ASPNETCORE_URLS=http://+:8080
{{/if}}