apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: {{name}}
spec:
  type: {{type}}
  version: {{version}}
{{#if emptyMetadata}}
  metadata: []
{{/if}}
{{#if hasMetadata}}
  metadata:
  {{#each metadata}}
    - name: {{@key}}
      value: {{this}}
  {{/each}}
{{/if}}